# دليل نشر AliPal مجانًا (بدون أي رسوم)

هذه الحزمة حوّلت تطبيق AliPal إلى **تطبيق ويب تقدمي (PWA)** حقيقي — وهذا هو الأساس الذي تحتاجه لتوليد ملف APK مجانًا ونشره على متاجر بديلة.

## محتويات الحزمة
- `index.html` — التطبيق نفسه (بعد إضافة دعم PWA)
- `manifest.json` — بيانات التطبيق (الاسم، الأيقونة، الألوان)
- `service-worker.js` — يجعل التطبيق يعمل بدون إنترنت بعد أول فتح
- `icons/` — أيقونات AliPal بجميع المقاسات المطلوبة

---

## الخطوة ١: استضف الملفات مجانًا (مطلوبة قبل أي شيء)
أي أداة تحوّل PWA إلى APK تحتاج **رابط إنترنت حقيقي** للتطبيق. الحل المجاني الأسهل هو GitHub Pages:

1. أنشئ حسابًا مجانيًا على github.com (إن لم يكن لديك).
2. أنشئ مستودعًا (Repository) جديدًا باسم مثلاً `alipal-app`.
3. ارفع الملفات الأربعة/المجلد بالكامل (`index.html`, `manifest.json`, `service-worker.js`, `icons/`) إلى المستودع.
4. من إعدادات المستودع Settings → Pages، فعّل GitHub Pages على الفرع main.
5. بعد دقيقة سيصبح تطبيقك متاحًا على رابط مثل:
   `https://your-username.github.io/alipal-app/`

*(بدائل مجانية أخرى: Netlify أو Cloudflare Pages — كلها مجانية بالكامل ولا تحتاج بطاقة ائتمان.)*

---

## الخطوة ٢: ولّد ملف APK مجانًا من الرابط
استخدم أداة **PWABuilder** المجانية من مايكروسوفت (لا تحتاج أي برمجة):

1. افتح: `https://www.pwabuilder.com`
2. الصق رابط GitHub Pages الخاص بك واضغط "Start".
3. ستفحص الأداة `manifest.json` و`service-worker.js` تلقائيًا (كلاهما جاهزان في هذه الحزمة).
4. اختر "Android" من قائمة المنصات، ثم "Generate Package".
5. ستحصل على ملف **APK وAAB موقّع (signed)** جاهز للنشر — مجانًا بالكامل.

---

## الخطوة ٣: انشر مجانًا على متاجر بديلة
بعد الحصول على ملف APK، سجّل في أي من هذه المتاجر (تسجيل مجاني ١٠٠٪، بدون رسوم اشتراك):

| المتجر | التسجيل | الرابط |
|---|---|---|
| **Samsung Galaxy Store** | مجاني بالكامل | seller.samsungapps.com |
| **Huawei AppGallery** | مجاني بالكامل | developer.huawei.com/consumer/en/console |
| **APKPure / Aptoide** | رفع مباشر بدون تسجيل معقّد | partner.apkpure.com |

كل ما عليك: إنشاء حساب مطوّر، تعبئة اسم التطبيق ووصفه وصور من الشاشات، ثم رفع ملف APK/AAB.

---

## الخطوة ٤ (بديل فوري): توزيع مباشر بدون متجر إطلاقًا
- ارفع ملف APK على موقعك أو Google Drive أو قناة تيليجرام/واتساب.
- أي مستخدم أندرويد يحمّله ويثبّته مباشرة (سيحتاج تفعيل "السماح بتثبيت من مصادر غير معروفة" مرة واحدة فقط).
- هذا مجاني تمامًا ولا يتطلب أي تسجيل في أي مكان.

---

## ملاحظة مهمة
منتجات وبيانات AliPal الحالية (الأسعار، الطلبات) هي بيانات تجريبية تُخزَّن في الذاكرة فقط (تختفي عند إغلاق التطبيق). قبل الإطلاق الفعلي للجمهور، ستحتاج ربط التطبيق بقاعدة بيانات حقيقية (مثل Firebase المجاني للمشاريع الصغيرة) لحفظ الطلبات والمنتجات بشكل دائم. أخبرني إن أردت المساعدة في هذه الخطوة التالية.
